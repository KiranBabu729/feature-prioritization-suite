# Auth is personal, via `ant auth login` (see README) — no API key needed here.
# If you ever want a service-account key instead, add it back and set
# apiKey/authToken from env in src/anthropicClient.js.
ANTHROPIC_MODEL=claude-opus-5
PORT=4001
